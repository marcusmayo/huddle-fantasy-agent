# Yahoo browser acceptance — September 9, 2026

## Fifth attempt: two acknowledged picks; continuation still failed

Free standard mock **Power Sweep 11105853**, seat 8, loaded `9d0faca91f203d7b94d9e025259905b2b41e6b89` with the same 250 dated candidates and eight-team/fifteen-round/30-second settings. Huddle was prepared before joining and the extra lobby was closed. The entry tool returned after 18.095 seconds with a Settings-click/CDP timeout. A fresh read verified the correct client, no settings overlay and forty seconds before drafting. An inspected keyboard sequence opened, verified and closed league settings before activating the controller at **06:52:33.992 UTC**. The entry helper was not blindly replayed.

The first invocation returned normally with no unsettled operation. After a **3.894-second** caller gap, both consecutive opening picks completed:

| Pick | Huddle recommendation and submitted player | Alternatives displayed | Saved plan / submission / matching receipt (UTC) |
| --- | --- | --- | --- |
| 8 | Jaxon Smith-Njigba, Yahoo ID 40041 | De'Von Achane, both styles | 06:53:14.604 / 06:53:15.536 / 06:53:16.976 |
| 9 | De'Von Achane, Yahoo ID 40118 | James Cook III, both styles | 06:53:18.422 / 06:53:19.562 / 06:53:21.050 |

Both plans had exact-turn revisions, all recommendation/decision panels visible at **640×720**, display confirmation before input, 27 seconds on the fresh pre-dispatch clock, returned exact-ID keyboard inputs and matching accepted identities. The controller's submission/verification phases took 1.618 and 1.670 seconds, including saving intent before input. The independent saved-evidence verifier passed each of these two pick checks and verified the audit/snapshot hashes. They are the first acknowledged real Yahoo submissions in this acceptance series.

The second invocation kept reconciling opponents, including board-change retries, and yielded at **06:53:34.876** after own pick 24 began because its remaining invocation budget could not cover a complete decision. The next invocation began at **06:53:44.477**, a **9.601-second gap**. A fresh observation at pick 24 showed nineteen seconds; after clock-age/skew allowance the guard reported seventeen seconds against a 24-second reserve. It stopped with no further input. Across the 251 recorded controller operations there were **zero deadline overruns**: observation maximum 376 ms, Results 1.213 seconds, preparation 725 ms, display 860 ms and submission 172 ms. The continuation failure cannot be described as another browser timeout or fixed by crediting Yahoo's automatic picks.

### Repairs and scoped verification

- Entry uses the successfully observed keyboard activation for Settings, League Settings and close. Each entry action records its stage before dispatch. Any later invocation refuses to replay a partially attempted entry; the operator must inspect the current state before further input. Two startup checks failed before this repair and now pass.
- After an acknowledged matching result, a window now yields while a fresh Results observation shows opponents selecting and the next consecutive pick is not ours. Adjacent owned picks stay in the same window. This gives the caller the opponent interval for continuation instead of consuming it and yielding on the next owned clock. A regression reproduced the previous late return. No clock reserve, operation cap or unsettled-input protection was reduced.
- A separate regression injects the observed 9.601-second gap and confirms that an undersized thirty-second turn still stops without input. A synthetic 75-second DR replay now includes that gap between windows and completes all twenty acknowledged picks/120 results. These simulations do not convert the failed actual mock into a pass or guarantee future tool timing.

The final focused controller/entry batch passed **45/45**. The final application suite passed **331/331**, zero failures/cancellations/skips, in **71.015 seconds**; core integrity passed. Logs: `.media-build/yahoo-entry-handoff-before.log`, `yahoo-owned-block-before.log`, `yahoo-entry-block-focused.log`, `yahoo-entry-block-full-suite.log` and `yahoo-entry-block-core-check.log`.

After the terminal stop, final reconciliation added 97 results to the 23 already saved. All **120 results and fifteen owned picks** are retained. Exactly two owned picks are acknowledged assistant selections; thirteen are accepted without plans and remain unattributed. Full manual acceptance was **rejected**. The initial completed-view read was 640×774; a separate 640×720 read and screenshot were visually checked with all fifteen owned picks and panels in frame. Neither replaces the two original on-clock display observations.

Evidence: `draft-day/yahoo-browser-acceptance-2026-09-09T06-45-52-908Z/`. The owned lobby/room/display tabs were closed, viewport reset, and temporary server exited with code 0. The independent recorder and unrelated tabs were preserved. No recording was started, no DR roster or published preview changed, and no current model/effort telemetry was invented. Recording remains optional external evidence. These local startup/scheduling repairs need another actual-run verification; uninterrupted manual execution, actual queue recovery, source/news completeness, sourced calibration and production-version acceptance remain open.

## Fourth attempt: an undersized read deadline and delayed continuation

Free standard mock **Three and Out 11105132**, seat 8, used local commit `38eaafda9699dafffe54a5c619f62847332dc9e0`, 250 previously dated candidates, eight teams, fifteen rounds and a 30-second clock. The explicit local draft-view route opened normally. Four alternating Huddle/Yahoo preflight reads took 13–30 ms and 25–40 ms respectively. These waiting-room measurements do not certify future on-clock timing. The entry helper handled the transition into the actual room and control activated at **06:32:21.407 UTC**.

The new timing traces identify a specific defect. Near the end of a 45-second invocation, an observation started at **06:33:05.302** with only about 233 ms left before the outer watchdog. It timed out at **06:33:05.535**, then settled at **06:33:05.698**, 396 ms after starting. The preceding light observations took 35–41 ms. This was an artificially shortened operation budget, not evidence of another 4.5-second browser stall. The invocation returned at **06:33:05.537** with the operation still unsettled. The next invocation began at **06:33:22.521**, a **16.984-second control gap**. That orchestration delay also contributed to the missed turn and is an assistant execution issue.

After settlement, fresh reads reconciled seven results and activated control at **06:33:24.925**. Own pick 8 then had fifteen seconds remaining against the controller's 24-second estimated reserve. The clock guard stopped execution; no reserve was relaxed. There were **zero Draft inputs, zero Queue inputs, zero display confirmations and zero assistant receipts**. All fifteen owned selections were Yahoo automatic picks. The independent saved-evidence verifier rejected manual acceptance. No stopped controller was resumed.

### Repair and verification

Each iteration now checks whether its invocation has enough time for the observation/workspace/heartbeat stages before starting another read. Pending-result verification similarly yields before starting a Results/reconciliation cycle with an undersized budget. The existing operation caps, uncertain-input fence, exact-identity checks and on-clock reserve remain enforced. The invoking operator must continue healthy windows promptly; this patch does not run computer use in the background or guarantee uninterrupted invocation.

Two regressions failed before the repair: waiting-loop observations received successively smaller deadlines, and pending-result verification kept starting within a window too short for that cycle. Both now pass. The overlap test was updated to admit its first read before exercising a competing invocation; it still verifies that the second invocation cannot replace the first deadline. The initial focused/full test processes were interrupted after the old test waited for a read that the new guard correctly declined. They are not passing runs. Final verification passed **29/29 controller tests** and **328/328 application tests**, zero failures/cancellations/skips, in **53.997 seconds** for the full suite. Core integrity passed. Logs: `.media-build/yahoo-window-admission-before.log`, `yahoo-window-admission-focused-final.log`, `yahoo-window-admission-full-suite-final.log` and `yahoo-window-admission-core-check.log`.

After stopping, final reconciliation imported the remaining 113 results for **120 total and fifteen owned picks**. The completed 640×720 view was visually checked with all panels and owned picks in frame; it states that the executor and pre-submission reason are unverified. Result/audit integrity and completed layout are separate from failed execution acceptance. Evidence: `draft-day/yahoo-browser-acceptance-2026-09-09T06-26-26-223Z/`.

The owned lobby, Yahoo room and Huddle display tabs were closed, the viewport reset, and the temporary service stopped. The user's recorder and unrelated tabs were preserved. No new recording was made, and recording remains optional external evidence with no Huddle connection or execution dependency. The DR roster, original video and published previews were unchanged. These fixes remain local. A new uninterrupted manual Yahoo run, actual queue mutation/recovery, comprehensive source/news coverage, calibration and production-version acceptance remain open. Later mock metadata does not claim independently verified Astra/Extra High telemetry; the historical real-draft configuration remains user-confirmed.

## Third attempt: one displayed decision, no acknowledged submission

Free standard mock **Pump Fake 11104442**, seat 8, used the same verified eight-team/fifteen-round/30-second rules and 250 dated candidates. The run loaded local commit `086666e3c7f838a79fe032758f9574869a4dfe85`. Before joining, the bare local origin returned `ERR_BLOCKED_BY_CLIENT` in the in-app browser, while the service remained healthy. Opening its explicit `/draft-view.html` route succeeded without changing browser protections. The origin served JSON rather than a dashboard; the browser's underlying blocking cause remains unconfirmed. The actual draft-view route and its reader were checked before joining.

The waiting room automatically navigated into the draft client and one gate read timed out (`Runtime.evaluate`, reported 645 ms). No session, controller or input existed then. A fresh read verified `/draftclient/f1/11104442/8` and 37 seconds until drafting. Settings verification and control activation then succeeded at **06:12:35.747 UTC**. The first window yielded normally. At **06:13:15.115**, a heartbeat rejected an expired lease. Fresh preparation activated a new run at **06:13:17.315**. At pick 8, Huddle's exact-turn **Jaxon Smith-Njigba** recommendation and selection were displayed with all panels in frame; one display-confirmed event was saved.

Two subsequent Yahoo observation calls exceeded the outer deadline before dispatch, settling after **4.518** and **5.739 seconds**. After read-only status confirmed settlement, the controller was explicitly stopped and its authenticated `input-not-dispatched` evidence saved. **There were zero Draft inputs, zero Queue inputs and zero assistant receipts.** Yahoo's automatic picks are not assistant submissions, even where the selected identity matches the cancelled plan. The precise cause of the intermittent read delays and lease gap was not established by the prior duration-only traces.

### Repairs after this failed attempt

- Clock/manual-mode observations now return only turn, route and Autodraft evidence. They skip player-table parsing and unrelated button layout reads. Exact-player preparation, Draft validation and Results still read the full required structures. In five read-only pairs after stopping, full reads took 121–624 ms and smaller reads 72–124 ms. The final adapter's five checks took **65–92 ms**. These measurements do not prove a bound under future on-clock conditions.
- Waiting-room read timeouts now yield an explicit retry before any settings/entry input; ended computer-use contexts still fail. The entry helper refuses to recreate an already prepared session or controller. It does not replay partially completed settings or entry actions.
- A returned Results activation later produced no visible selection change and the operation exhausted its budget. A fresh inspected keyboard activation recovered it. Navigation now permits at most one retry after a returned input and fresh unchanged-state/room verification, leaving time within the same budget. This applies only to idempotent tabs, never Draft or Queue, and does not retry rejected/uncertain input operations. Changed rooms and persistent failure still stop the operation.
- Controller status now retains bounded operation and invocation traces: starts, actual settlement, deadline overruns, outcomes and dispatched-input flags. Late settlement is distinguishable from cancellation. No old runtime trace is retroactively reconstructed, and no unsettled-operation protection or clock reserve was relaxed.

After the stop, final reconciliation added the remaining 113 picks to the seven already imported. All **120 results and fifteen owned picks** were saved. The completed 640×720 Huddle view retained all fifteen owned picks and all panels in frame. An independent evidence check verified the result prefix and audit/snapshot hashes but **rejected manual acceptance**: one plan, one confirmed display and zero acknowledged submissions. The final repaired adapter completed a read-only Players→Results round trip in **2.131 seconds**; no retry was needed in that check.

Four new entry/turn-reader regressions failed before repair. The focused entry/reader/controller batch passed 50 tests, and the final navigation batch passed 11. Final application validation passed **326/326**, zero failures/cancellations/skips, in **53.794 seconds**; core integrity passed. Evidence: `draft-day/yahoo-browser-acceptance-2026-09-09T06-05-10-434Z/`. Logs: `.media-build/yahoo-turn-read-before.log`, `yahoo-turn-read-focused.log`, `yahoo-turn-navigation-focused.log`, `yahoo-turn-read-full-suite.log` and `yahoo-turn-read-core-check.log`.

The owned lobby, room and display tabs were closed, the viewport reset, and the temporary service exited with code 0. User tabs and the independent recorder were preserved. No recording was started or linked to Huddle, no DR roster was changed, and the original video/published previews were untouched. These local repairs improve the observed paths; uninterrupted manual execution, actual queue mutation/recovery, comprehensive source/news coverage and production acceptance remain open.

## Second attempt: activated, then stopped without submissions

Free standard mock **Flea Flicker 11103545**, seat 8, was entered with verified eight-team/fifteen-round/30-second/half-PPR settings and a prepared 250-candidate pool. Original source dates and the estimated scoring basis were retained. The actual waiting-room popup was identified and its lobby closed. The isolated local Huddle session activated control at **05:47:28.401 UTC** on September 9. This is additional startup evidence, not a resolution of the first attempt's unexplained route mismatch.

The first bounded window failed at **05:48:07.642 UTC** with `OPERATION_TIMEOUT` while reading Results. There were no pending inputs or submission receipts. Automatic approval review rejected continuing while that read remained unsettled because continuation could duplicate or misapply an action. A later read-only status confirmed settlement; the next room observation was already own pick 8 with three seconds left. The controller was explicitly stopped, its service stop was confirmed, and it was never resumed. **Zero Draft inputs and zero Queue inputs were made.** Yahoo completed all fifteen owned picks automatically. This was a failed execution acceptance.

| Finding | Repair and verification | Limit |
| --- | --- | --- |
| Results, Players and Round by Round are native buttons with accessible role `tab`; the old reader targeted role `button`. | Navigation uses the observed unique control's role and keyboard activation. Selected state, delayed subtabs and the expected table must settle within one deadline; input is not repeated while waiting. | A correct-role pointer click returned without selecting Results; its underlying cause is unconfirmed. Continuous on-clock navigation still needs acceptance. |
| Yahoo changes the countdown to a lone integer near expiry. | Accept short seconds only immediately before the current round/pick label. Unrelated integers, malformed seconds and competing clocks remain rejected. | Reading three seconds does not authorize a late submission; execution reserves remain enforced. |
| A window could start multi-step reconciliation with too little invocation time left. | Yield before Results/reconciliation when the remaining window cannot cover the measured stages. The adapter deadline leaves transport settlement time inside the outer watchdog. Status identifies an unsettled operation. | A timeout still does not prove cancellation; unresolved operations remain fenced. |
| Huddle BODY locator evaluation timed out on the visible in-app page, while direct document evaluation succeeded. | The page display adapter uses document evaluation. Revision waiting, the 400-ms stability interval and the final read share a bounded deadline and respect cancellation. | Browser verification covered the completed display reader, not an actual on-clock display-confirmed submission. The underlying locator failure is unconfirmed. |

After stopping, keyboard-based Results inspection recovered the full 120-pick prefix with fifteen owned identities. The patched adapter completed a Players→Results round trip in **1.975 seconds** on the completed room. Reconciliation imported all 120 into the isolated Huddle session. At **640×720**, the repaired display reader reported all panels in frame and all fifteen owned picks. The UI marks the latest selection's executor unverified and states that no pre-submission recommendation/reason was retained. Reconciliation and layout are verified separately from execution.

Four navigation/clock/window regressions failed before repair. Their focused batch passed 35 tests; the delayed-subtab extension passed all eight adapter tests. Three additional display regressions cover unavailable BODY locators, wrong/stale/clipped/changing decisions, and shared deadlines/cancellation. The combined adapter/display batch passed 11 tests. The final full suite passed **321/321**, zero failures, cancellations or skips, in **53.584 seconds**. Core integrity passed. Logs: `.media-build/yahoo-navigation-before.log`, `yahoo-navigation-focused.log`, `yahoo-display-focused.log`, `yahoo-navigation-display-full-suite.log` and `yahoo-navigation-core-check.log`.

Evidence directory: `draft-day/yahoo-browser-acceptance-2026-09-09T05-42-05-085Z/`. It retains preparation, the failed window, settled recovery, explicit stop, actual role diagnostics, raw terminal Results, final reconciliation, repaired navigation, failed/repaired display reads and the exported audit. The owned lobby and room/display tabs were closed, the viewport reset, and the acceptance server exited with code 0. The user's original recorder and unrelated tabs were preserved.

Recording was neither started nor required. It is optional external evidence, with no connection to Huddle readiness or execution. These changes remain local and do not alter the DR roster, historical attribution, source video or published previews. A fully verified manual Yahoo draft, actual queue mutation/recovery, comprehensive source/news coverage and production acceptance remain open.

## First attempt and subsequent source preparation

**Later source preparation:** ten read-only player-list pages now provide 250 distinct candidates with enough positional depth for the standard mock. Their source dates, injury badges and estimated scoring conversions are retained. See [dated candidate preparation and scoring limits](yahoo-season-evidence.md). This was not a second timed draft: no session/controller was started and no Draft/Queue input was made. The earlier failed startup described below remains unaccepted.

**Result: startup failed; clean live acceptance remains open.** This was a free Yahoo standard mock, Crackback Block **11101960**, seat **8**, eight teams, fifteen rounds and 120 total selections. The entry helper verified a 30-second pick clock, half-PPR, four-point passing touchdowns, the standard roster and position caps. This does not test the DR league's six-team, twenty-round scoring configuration.

The maintained controller had not been created, no Huddle rehearsal session had been prepared, and no manual Draft or Queue input was issued. Yahoo's independent automatic selections must not be credited to Huddle or the assistant. The attempt was abandoned after startup failed. Subsequent browser reads inspected the defect; they were not a resumed draft run. No recording was started or required.

## Findings and repairs

| Finding | Evidence and resolution | Remaining acceptance |
| --- | --- | --- |
| Join opened a separate waiting-room tab | The lobby remained open. The waiting-room tab was selected explicitly and its room/seat verified. | A launcher must resolve the actual new tab and retain its identity; the lobby is never the executing room. |
| Startup rejected the room route after settings verification | The original failing observation was not retained. Later observations matched the intended Yahoo origin and `/draftclient/f1/11101960/8`. The underlying cause is unconfirmed. Mismatch errors now retain expected and observed origin/path; controller faults preserve those fields. Authentication query strings are not captured. | Reproduce and resolve the actual handoff failure. Do not remove the identity guard or infer an Edge security cause from this error. |
| Turn separators were parsed as players | Yahoo places TH-only “Your Turn” rows inside TBODY. The old reader returned 112 rows: 100 identified players and 12 separators, and included the separators in its column headings. A regression reproduced `PLAYER_IDENTITY_UNREADABLE`. The reader now uses THEAD columns and direct data rows/cells; retained legacy snapshots ignore empty divider rows. Malformed nonempty rows and duplicate identities still fail. | The repaired reader passed a read-only check on actual Yahoo: 20 proper columns, 100 parsed players and 100 unique numeric identities. This is not selection/timing acceptance. |
| The queue button has no accessible “Queue” name | Actual DOM showed an unnamed star button inside `.ys-addqueue[data-id]`. The adapter now scopes that observed container to the intended numeric player identity. Contract tests preserve an existing queue entry, accept a verified result after a lost response, and remove only a wrong new addition. | The actual queue container, removal control and live mutation/recovery still need verification. No real queue input was made in this attempt. |
| Only 100 candidates were loaded | The isolated bootstrap required at least 120 observed identities. Even after the row repair, this first page is insufficient. That requirement was not lowered; no session was created from it. | Prepare sufficient dated, identified candidates and positional depth before entry. The first rendered page cannot certify a full Yahoo universe or DR scoring readiness. |

The initial suggestion that the extra rows were nested draft-board rows was corrected after inspecting their actual HTML: they were direct TH-only separator rows in the player table. The new direct table collections also avoid accidentally collecting nested detail rows/cells.

## Verification scope

Two regressions failed before the repair: player preparation with a turn separator, and missing route diagnostics. The focused adapter/mock/controller batch passed 41 tests; a subsequent controller diagnostic check passed with its complete 25-test controller batch. The final full suite passed **299/299 tests**, zero failures, cancellations or skips, in **52.837 seconds**; the fleet-core integrity check also passed. These checks include the existing synthetic twenty-selection/120-result replay without recording. They do not replace the failed actual mock.

Local evidence is retained in `draft-day/yahoo-browser-acceptance-2026-09-09T05-04-47-467Z/failed-startup.json` and `repaired-reader-browser-check.json`. The earlier bootstrap server never created a session. Both temporary servers and the three created browser tabs were cleaned up, the viewport override reset, and the user's existing recorder and unrelated tabs left intact.

The repairs are local and are not deployed by publishing the preview videos. Historical DR pick attribution, the user-submitted Gibbs pick, Yahoo's grade comparison and the user-confirmed Astra/Extra High/computer-use context remain unchanged. Recording is optional external evidence documenting Huddle and ChatGPT computer use; it is not an application connection or an execution prerequisite.
