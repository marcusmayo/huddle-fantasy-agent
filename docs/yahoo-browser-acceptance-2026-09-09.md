# Yahoo browser acceptance — September 9, 2026

## Second attempt: activated, then stopped without submissions

Free standard mock **Flea Flicker 11103545**, seat 8, was entered with verified eight-team/fifteen-round/30-second/half-PPR settings and a prepared 250-candidate pool. Original source dates and the estimated scoring basis were retained. The actual waiting-room popup was identified and its lobby closed. The isolated local Huddle session activated control at **05:47:28.401 UTC** on September 9. This is additional startup evidence, not a resolution of the first attempt's unexplained route mismatch.

The first bounded window failed at **05:48:07.642 UTC** with `OPERATION_TIMEOUT` while reading Results. There were no pending inputs or submission receipts. Automatic approval review rejected continuing while that read remained unsettled because continuation could duplicate or misapply an action. A later read-only status confirmed settlement; the next room observation was already own pick 8 with three seconds left. The controller was explicitly stopped, its service stop was confirmed, and it was never resumed. **Zero Draft inputs and zero Queue inputs were made.** Yahoo completed all fifteen owned picks automatically. This was a failed execution acceptance.

| Finding | Repair and verification | Limit |
| --- | --- | --- |
| Results, Players and Round by Round are native buttons with accessible role `tab`; the old reader targeted role `button`. | Navigation uses the observed unique control's role and keyboard activation. Selected state, delayed subtabs and the expected table must settle within one deadline; input is not repeated while waiting. | A correct-role pointer click returned without selecting Results; its underlying cause is unconfirmed. Continuous on-clock navigation still needs acceptance. |
| Yahoo changes the countdown to a lone integer near expiry. | Accept short seconds only immediately before the current round/pick label. Unrelated integers, malformed seconds and competing clocks remain rejected. | Reading three seconds does not authorize a late submission; execution reserves remain enforced. |
| A window could start multi-step reconciliation with too little invocation time left. | Yield before Results/reconciliation when the remaining window cannot cover the measured stages. The adapter deadline leaves transport settlement time inside the outer watchdog. Status identifies an unsettled operation. | A timeout still does not prove cancellation; unresolved operations remain fenced. |
| Huddle BODY locator evaluation timed out on the visible in-app page, while direct document evaluation succeeded. | The page display adapter uses document evaluation. Revision waiting, the 400-ms stability interval and the final read share a bounded deadline and respect cancellation. | Browser verification covered the completed display reader, not an actual on-clock display-confirmed submission. The underlying locator failure is unconfirmed. |

After stopping, keyboard-based Results inspection recovered the full 120-pick prefix with fifteen owned identities. The patched adapter completed a Players→Results round trip in **1.975 seconds** on the completed room. Reconciliation imported all 120 into the isolated Huddle session. At **640×720**, the repaired display reader reported all panels in frame and all fifteen owned picks. The UI marks the latest selection's executor unverified and states that no pre-submission recommendation/reason was retained. Reconciliation and layout are verified separately from execution.

Four navigation/clock/window regressions failed before repair. Their focused batch passed 35 tests; the delayed-subtab extension passed all eight adapter tests. Three additional display regressions cover unavailable BODY locators, wrong/stale/clipped/changing decisions, and shared deadlines/cancellation. The combined adapter/display batch passed 11 tests. The final full suite passed **321/321**, zero failures, cancellations or skips, in **53.584 seconds**. Core integrity passed. Logs: `.media-build/yahoo-navigation-before.log`, `yahoo-navigation-focused.log`, `yahoo-display-focused.log`, `yahoo-navigation-display-full-suite.log` and `yahoo-navigation-core-check.log`.

Evidence directory: `draft-day/yahoo-browser-acceptance-2026-09-09T05-42-05-085Z/`. It retains preparation, the failed window, settled recovery, explicit stop, actual role diagnostics, raw terminal Results, final reconciliation, repaired navigation, failed/repaired display reads and the exported audit. The owned lobby and room/display tabs were closed, the viewport reset, and the acceptance server exited with code 0. The user's original recorder and unrelated tabs were preserved.

Recording was neither started nor required. It is optional external evidence, with no connection to Huddle readiness or execution. These changes remain local and do not alter the DR roster, historical attribution, source video or published previews. A fully verified manual Yahoo draft, actual queue mutation/recovery, comprehensive source/news coverage and production acceptance remain open.

## First attempt and subsequent source preparation

**Later source preparation:** ten read-only player-list pages now provide 250 distinct candidates with enough positional depth for the standard mock. Their source dates, injury badges and estimated scoring conversions are retained. See [dated candidate preparation and scoring limits](yahoo-season-evidence.md). This was not a second timed draft: no session/controller was started and no Draft/Queue input was made. The earlier failed startup described below remains unaccepted.

**Result: startup failed; clean live acceptance remains open.** This was a free Yahoo standard mock, Crackback Block **11101960**, seat **8**, eight teams, fifteen rounds and 120 total selections. The entry helper verified a 30-second pick clock, half-PPR, four-point passing touchdowns, the standard roster and position caps. This does not test the DR league's six-team, twenty-round scoring configuration.

The maintained controller had not been created, no Huddle rehearsal session had been prepared, and no manual Draft or Queue input was issued. Yahoo's independent automatic selections must not be credited to Huddle or the assistant. The attempt was abandoned after startup failed. Subsequent browser reads inspected the defect; they were not a resumed draft run. No recording was started or required.

## Findings and repairs

| Finding | Evidence and resolution | Remaining acceptance |
| --- | --- | --- |
| Join opened a separate waiting-room tab | The lobby remained open. The waiting-room tab was selected explicitly and its room/seat verified. | A launcher must resolve the actual new tab and retain its identity; the lobby is never the executing room. |
| Startup rejected the room route after settings verification | The original failing observation was not retained. Later observations matched the intended Yahoo origin and `/draftclient/f1/11101960/8`. The underlying cause is unconfirmed. Mismatch errors now retain expected and observed origin/path; controller faults preserve those fields. Authentication query strings are not captured. | Reproduce and resolve the actual handoff failure. Do not remove the identity guard or infer an Edge security cause from this error. |
| Turn separators were parsed as players | Yahoo places TH-only “Your Turn” rows inside TBODY. The old reader returned 112 rows: 100 identified players and 12 separators, and included the separators in its column headings. A regression reproduced `PLAYER_IDENTITY_UNREADABLE`. The reader now uses THEAD columns and direct data rows/cells; retained legacy snapshots ignore empty divider rows. Malformed nonempty rows and duplicate identities still fail. | The repaired reader passed a read-only check on actual Yahoo: 20 proper columns, 100 parsed players and 100 unique numeric identities. This is not selection/timing acceptance. |
| The queue button has no accessible “Queue” name | Actual DOM showed an unnamed star button inside `.ys-addqueue[data-id]`. The adapter now scopes that observed container to the intended numeric player identity. Contract tests preserve an existing queue entry, accept a verified result after a lost response, and remove only a wrong new addition. | The actual queue container, removal control and live mutation/recovery still need verification. No real queue input was made in this attempt. |
| Only 100 candidates were loaded | The isolated bootstrap required at least 120 observed identities. Even after the row repair, this first page is insufficient. That requirement was not lowered; no session was created from it. | Prepare sufficient dated, identified candidates and positional depth before entry. The first rendered page cannot certify a full Yahoo universe or DR scoring readiness. |

The initial suggestion that the extra rows were nested draft-board rows was corrected after inspecting their actual HTML: they were direct TH-only separator rows in the player table. The new direct table collections also avoid accidentally collecting nested detail rows/cells.

## Verification scope

Two regressions failed before the repair: player preparation with a turn separator, and missing route diagnostics. The focused adapter/mock/controller batch passed 41 tests; a subsequent controller diagnostic check passed with its complete 25-test controller batch. The final full suite passed **299/299 tests**, zero failures, cancellations or skips, in **52.837 seconds**; the fleet-core integrity check also passed. These checks include the existing synthetic twenty-selection/120-result replay without recording. They do not replace the failed actual mock.

Local evidence is retained in `draft-day/yahoo-browser-acceptance-2026-09-09T05-04-47-467Z/failed-startup.json` and `repaired-reader-browser-check.json`. The earlier bootstrap server never created a session. Both temporary servers and the three created browser tabs were cleaned up, the viewport override reset, and the user's existing recorder and unrelated tabs left intact.

The repairs are local and are not deployed by publishing the preview videos. Historical DR pick attribution, the user-submitted Gibbs pick, Yahoo's grade comparison and the user-confirmed Astra/Extra High/computer-use context remain unchanged. Recording is optional external evidence documenting Huddle and ChatGPT computer use; it is not an application connection or an execution prerequisite.
