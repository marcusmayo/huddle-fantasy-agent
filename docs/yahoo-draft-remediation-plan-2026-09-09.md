# Draft remediation plan — confirmed by the user

Outcome: Huddle must deliver fresh, usable recommendations independently of ChatGPT, leaving a human at least ten seconds to select. A successful final roster is not evidence of timely recommendations.

1. **Freeze and reproduce the actual execution path.** Preserve mock 11172913 evidence. Replace runtime method reassignment and duplicate adapters with one explicit, versioned runner construction. Record the loaded build identity. Replay recorded owned-turn observations through the entire capture → parse → import → display path. Test both Queue and Draft headings, full result prefixes, defense identity, stale observations, and tab/turn transitions. No editing or reloading during an acceptance draft.
2. **Resolve independent source delivery before another live test.** Verify what the supported Yahoo integration actually supplies and how quickly it supplies it, including clock provenance. Use the app's existing poller and push stream where supported. Do not introduce a required extension or depend on agent calls. If a supported self-contained path cannot supply sufficiently fresh mock data and timing, stop here and report the exact missing capability; do not substitute a simulated success or another call-driven mock. A continuous local fixture verifies independence but cannot establish Yahoo support.
3. **Enforce a clock-based human deadline.** Calculate usable time from the source clock observation, its age and a safety allowance. Reserve ten seconds for human input plus two seconds of delivery margin. For a 30-second turn, target visible recommendations by elapsed 18 seconds or earlier; for 70 seconds, by 58 seconds or earlier. Revalidate on every turn and clock change. Unverified clocks must not produce a ready claim. A late, stale or disconnected feed must produce a visible warning with the last verified timestamp, rather than an apparently current recommendation.
4. **Make interruption and recovery explicit.** Separate observing, recommending, awaiting human selection, reconciling, blocked and completed states. Monitor feed health independently of the agent. Detect Yahoo inactivity/autodraft when the integration exposes reliable evidence. Reconcile first after reconnect, then recompute; do not repeat uncertain submissions. Restoring manual mode cannot mark the system ready by itself. Huddle continues to advise a human; automated selection remains a separate test responsibility.
5. **Correct acknowledgement and evidence reporting.** Recognize final completion by session identity, expected board count and completed state instead of a nonexistent numeric current pick. Separate stored recommendation calculations from verified visible, actionable recommendations. Record source observation, app receipt, computation, visible display, remaining clock, selection and acknowledgement separately. Do not count setup/completion entries or autodrafts as recommendation/selection successes.
6. **Validate in stages and stop on failure.** Run focused regression tests, then sustained app delivery with ChatGPT idle and reconnection/clock-change cases. Only after those gates and a verified Yahoo source pass, run one Yahoo 30-second mock through completion. Acceptance requires every owned turn to show valid recommendations with at least ten seconds remaining, all selections acknowledged, zero unplanned autodrafts, and complete final reconciliation. Report every missed turn and timing sample. A failed gate ends pursuit of another mock pending review.

Order: execution-path correctness and independent continuity first; recovery and honest evidence next; no scoring revisions in this work. Existing local parser correction remains unaccepted in Yahoo. The user confirmed this plan in the current conversation. Implementation is authorized; live acceptance remains gated on verified independent source delivery.

Video work is separately requested and can proceed now: replace league/personal identifiers with Yahoo and Blitzkrieg in both recent edits, including recorded-screen identifiers, titles, captions, narration, previews and metadata. Keep Huddle/ChatGPT product attribution and player names as relevant editorial content. Preserve original evidence locally, retain each edit's voice/music/style and export formats, publish only the revised media/documentation to GitHub main, and record this naming rule for future video production.
